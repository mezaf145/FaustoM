// Lab 7 10.3
//Fausto Meza

#include "pch.h"
#include <iostream>
#include <cassert>
#include <string>
using namespace std;
string s;
//This function accepts a pointer to a c-string as an argument
//and returns the no. of words contained in the string.
//Input
//s - the string the user input 
//Output
//The number of words in the input
int countwords_s(string s);
int eat_word(string s);
void test_eat_word();
void test_countwords_s();
int main(void)
{
	test_countwords_s();
	cout << "Enter a string sequence" << endl;
	cin >> s;
	cout << countwords_s(s);
	return 0;
}
void test_eat_word()
{
	assert(eat_word("Where is my water") == 4);
	assert(eat_word("") == 0);
	assert(eat_word("\n Where is my water") == 4);
	assert(eat_word("\n") == 0);
	assert(eat_word(" Where is my water") == 4);
}

void test_countwords_s()
{
	assert(countwords_s("The earth is not exactly cool")== 6);
	assert(countwords_s("\n The earth is not exactly cool")== 6);
	assert(countwords_s(" ") == 0);
	assert(countwords_s("\n ")== 0);
	assert(countwords_s("Theearth is not exactly cool")== 5);
}
int eatword(string s)
{
	int w = 0;
	while(s.compare(" "))
	{
		w = w + 1;
	}
	return w;
}
int countwords_s(string s)
{
	int i = 0;
	int x = 0;
	while (!s.compare("")) {
		i = i + 1;
		s = eat_word(s);
		x = x - 1;
	}
	return x;
}
